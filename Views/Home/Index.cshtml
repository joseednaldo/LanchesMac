@model HomeViewModel
@await Html.PartialAsync("_Carrousel")

<h2>Os preferidos da semana</h2>
<h4>Tá com fome ???</h4>

<div class="row">
    @foreach (var lanche in Model.LanchePreferidos)
    {
        @Html.Partial("_LanchesResumo",lanche)
    }
</div>